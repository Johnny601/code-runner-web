type DemoProblem = {
  id: number;
  pathname: string;
  name: string;
  difficulty: string;
  solved: boolean;
};

type ProblemDetails = {
  problemId: number;
  problemName: string;
  description: string;
  progLang: string;
  codeTemplate: string;
};
