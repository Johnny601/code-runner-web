type DemoProblem = {
  id: number;
  pathname: string;
  name: string;
  difficulty: string;
};

type ProblemCodeTemplate = {
  problemId: number;
  problemName: string;
  progLang: string;
  codeTemplate: string;
};
